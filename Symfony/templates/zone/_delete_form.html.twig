<form method="post" action="{{ path('zone_delete', {'id': zone.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer cet item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ zone.id) }}">
    <button class="btn">Supprimer</button>
</form>
